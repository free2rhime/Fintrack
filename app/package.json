{
  "name": "fintrack-firestore-rules-tests",
  "version": "1.0.0",
  "description": "FinTrack Firestore Security Rules and Emulator Tests",
  "main": "index.js",
  "scripts": {
    "test": "npx mocha -r ts-node/register tests/**/*.test.ts --timeout 10000",
    "test:emulator": "firebase emulators:exec --only firestore 'npm test'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.0.0",
    "mocha": "^10.8.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
